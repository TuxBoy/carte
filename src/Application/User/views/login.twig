{% extends 'layout.twig' %}

{% block body %}

   <div class="container" style="padding-top: 75px;">
      <div class="card">
          <div class="card-header">
              Se connecter
          </div>
          <div class="card-body">
              {{ formBuilder(userForm) }}
          </div>
      </div>
   </div>

{% endblock %}